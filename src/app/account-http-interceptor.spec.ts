import { AccountHttpInterceptor } from './account-http-interceptor';

describe('AccountHttpInterceptor', () => {
  it('should create an instance', () => {
    expect(new AccountHttpInterceptor()).toBeTruthy();
  });
});
